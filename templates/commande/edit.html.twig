{% extends 'baseback.html.twig' %}

{% block title %}Edit commande{% endblock %}

{% block content %}
{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <a href="{{ path('app_Commande') }}">back to list</a>

                            <form class="user">
                                <div class="form-group row">
                                    <div class="col-sm-6 mb-3 mb-sm-0">
                                    {{ form_row(form.Wallet, { 'attr': {'class': 'form-control form-control-user', 'placeholder': 'Wallet Address'} }) }}                                    </div>
                                </div>
                                <div class="form-group">
                                    {{ form_row(form.email, { 'attr': {'class': 'form-control form-control-user', 'placeholder': 'Email Address'} }) }}
                                </div>
                                    <button type="submit" class="btn btn-primary btn-user btn-block">{{ button_label|default('Save') }}</button>

                                <hr>
                            </form>









{{ form_end(form) }}

{% endblock %}
