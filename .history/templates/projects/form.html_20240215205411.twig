{{ form_start(form) }}
    {{ form_start(form) }}

    {{ form_widget(form.nom) }}

    {{ form_widget(form.Description) }}

    {{ form_widget(form.Category) }}

    {{ form_widget(form.WalletAddress) }}

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
