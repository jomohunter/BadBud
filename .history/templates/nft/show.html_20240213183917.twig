{% extends 'base.html.twig' %}

{% block title %}NFT{% endblock %}

{% block body %}
    <h1>NFT</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ n_f_t.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ n_f_t.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ n_f_t.price }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ n_f_t.status }}</td>
            </tr>
            <tr>
                <th>CreationDate</th>
                <td>{{ n_f_t.CreationDate ? n_f_t.CreationDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ n_f_t.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_n_f_t_index') }}">back to list</a>

    <a href="{{ path('app_n_f_t_edit', {'id': n_f_t.id}) }}">edit</a>

    {{ include('nft/_delete_form.html.twig') }}
{% endblock %}
